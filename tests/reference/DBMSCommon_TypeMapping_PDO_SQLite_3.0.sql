PDO_SQLite_3.0                  : -- ColumnDeclaration
small binary with length        : "column" BLOB(2)
binary without length           : "column" BLOB
integer primary key             : "column" INTEGER NOT NULL
int. primary key auto increment : "column" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT
int auto increment              : "column" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT
int. composite PK auto inc.     : "column" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT
type with dflt length           : "column" bit(64)
