PostgreSQL_10.0                 : -- ColumnDeclaration
small binary with length        : "column" bytea NULL
binary without length           : "column" bytea NULL
integer primary key             : "column" bigint NOT NULL
int. primary key auto increment : "column" serial NOT NULL
int auto increment              : "column" serial NOT NULL
int. composite PK auto inc.     : "column" serial NOT NULL
type with dflt length           : "column" bit(64) NULL
